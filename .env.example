# Used only by the local dev proxy (npm run dev). In production these are set
# on the Cloudflare Pages project, with TELEMETRY_API_KEY stored as a Secret.
# Do not prefix them with VITE_: that would compile them into the public JS.
TELEMETRY_API_BASE=https://telemetry.burnthe.network
TELEMETRY_API_KEY=
