# Base URL of the telemetry service (no trailing path).
VITE_TELEMETRY_API_BASE=https://telemetry.burnthe.network

# Optional. Anything prefixed VITE_ is compiled into the public JS bundle,
# so leave this empty for public deploys and enter the key in the app instead.
VITE_TELEMETRY_API_KEY=
